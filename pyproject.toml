[project]
name = "reddoodle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "reddoodle", email = "reddoodle525@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[tool.maturin]
module-name = "reddoodle._core"
python-packages = ["reddoodle"]
python-source = "src"

[tool.uv]
cache-keys = [{ file = "pyproject.toml" }, { file = "src/**/*.rs" }, { file = "Cargo.toml" }, { file = "Cargo.lock" }]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
